{# @var plugin \brightlabs\craftsalesforce\Salesforce #}
{# @var settings \brightlabs\craftsalesforce\models\Settings #}

{% import '_includes/forms.twig' as forms %}

{{ forms.textField({
    first: true,
    label: 'Bearer token',
    name: 'bearerToken',
    value: settings.bearerToken
  }) }}

{# ... #}
